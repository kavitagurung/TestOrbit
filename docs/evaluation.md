# Evaluation

The synthetic evaluation plan measures relevant-signal precision, false-positive rate, duplicate accuracy, evidence coverage, citation accuracy, capability agreement, claim-status accuracy, trend precision, AI schema-validity rate, and fallback success. No performance values are claimed until a labeled evaluation dataset is run.

