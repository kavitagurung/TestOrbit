# MCP security

The proposed MCP server authenticates callers and exposes only named read-only intelligence queries. It cannot fetch arbitrary URLs, access secrets, mutate data, post messages, or create tickets. Any future write action requires separate human-approval design.

