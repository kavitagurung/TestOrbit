# Deployment guides

Frontend: GitHub Pages. Backend: Render. Database: Supabase PostgreSQL. Scheduled collection: GitHub Actions invoking a protected endpoint. Teams: one-way Workflows webhook stored only as `TEAMS_WEBHOOK_URL` in backend environment configuration. Deployment, external accounts, and billing remain intentionally unconfigured.

