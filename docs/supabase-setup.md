# Supabase setup guide

Create a PostgreSQL project only after approval, then set `DATABASE_URL` as a server-side secret. Run reviewed Alembic migrations. Do not place Supabase credentials in the frontend.

