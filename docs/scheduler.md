# Scheduler guide

Use GitHub Actions to invoke a protected backend collection endpoint on a controlled schedule. Authenticate with `SCHEDULER_TOKEN`, rate-limit source requests, and record source health and audit events.

