# Local setup guide

Use `npm install && npm run dev` for the frontend. In `apps/api`, create a virtual environment, install `requirements.txt`, then run `uvicorn app.main:app --reload`. The app runs entirely in synthetic demo mode without credentials.

