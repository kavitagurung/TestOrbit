# API

`GET /health`, `GET /api/v1/signals`, `GET /api/v1/competitors`, `GET /api/v1/brief`, `POST /api/v1/claim-lens`, and `GET /api/v1/scores/example` are available in the synthetic demo. All returned entities are clearly synthetic.

